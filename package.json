{
  "name": "brainrot-bot",
  "version": "1.0.0",
  "description": "A Discord bot like cat bot, but for Italian brainrot characters. Pulls from indieun.com/cab data.",
  "main": "index.js",
  "type": "commonjs",
    "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development node --watch index.js",
    "deploy": "node deploy-commands.js",
    "check": "node -c index.js && node -c deploy-commands.js && for f in src/*.js; do node -c \"$f\"; done && echo OK",
    "lint": "eslint index.js deploy-commands.js src/*.js"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "discord.js": "^14.27.0",
    "dotenv": "^16.4.5",
    "v2componentsbuilder": "^1.2.2"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
